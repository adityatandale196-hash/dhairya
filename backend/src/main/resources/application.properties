spring.application.name=backend
server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5432/dhairya
spring.datasource.url=${SPRING_DATASOURCE_URL}

spring.jpa.hibernate.ddl-auto=none
fast2sms.api.key=${FAST2SMS_API_KEY}git add .
git commit -m "Add automatic SMS via Fast2SMS"
git push